import 'package:flutter/material.dart';

void main() => runApp(MaterialApp(
  home: Scaffold(
    appBar: AppBar(title: Text('Hairstyle App')),
    body: Center(child: Text('Flutter v2 Ready')),
  ),
));